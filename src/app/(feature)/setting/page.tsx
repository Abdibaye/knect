import UserSettings from "@/components/settings/settings";
import AccountPage from "@/components/settings/settings";

import React from "react";

const page = () => {
  return <UserSettings />;
};

export default page;
